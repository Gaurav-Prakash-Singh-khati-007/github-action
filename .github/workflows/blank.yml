name: First-Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: print("Hello world") 
      - name: Print gud bye
        run: print("Bye")
